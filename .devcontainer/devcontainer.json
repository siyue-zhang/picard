// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile
{
    "name": "my_picard",
    "build": {
        // Sets the run context to one level up instead of the .devcontainer folder.
        // "context": "..",
        // Update the 'dockerFile' property if you aren't using the standard 'Dockerfile' filename.
        "dockerfile": "./Dockerfile"
    },
    "containerEnv": {
        "NVIDIA_VISIBLE_DEVICES": "2,3"
    },
    "runArgs": [
        "--runtime=nvidia"
    ],
    "customizations": {
        "vscode": {
          "extensions": [
            "ms-python.python", 
            "ms-azuretools.vscode-docker"]
        }
    },
    // "mounts": [
    //     "source=/home/ali-siyue/Projects/tableQA_text_to_SQL/data,target=/workspaces/my_picard/data,type=bind,consistency=cached",
    //     "source=/home/ali-siyue/Projects/tableQA_text_to_SQL/third_party,target=/workspaces/my_picard/main_third_party,type=bind,consistency=cached"
    // ]
}


